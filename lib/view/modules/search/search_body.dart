import 'package:flutter/material.dart';

class SearcBody extends StatelessWidget {
  const SearcBody({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}